spring:
  application:
    name: order-service
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: 'update'

server:
  port: ${SERVER_PORT:8080}